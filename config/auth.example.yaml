git_auth:
  preferred_transport: ssh
  fallback: credential_helper
  forbid_tokens_in_tracked_files: true

host_auth:
  method: ssh_agent
  bootstrap_method: host_password_once
  prefer_agent_forwarding: true
