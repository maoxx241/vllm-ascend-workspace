host_auth:
  mode: bootstrap-password-then-ssh-key
  credential_groups:
    shared-lab-a:
      username: root
      source: prompt-or-local-secret

git_auth:
  mode: ssh-agent-forwarding
  fallback:
    mode: local-credential-provider-plugin
