interface:
  display_name: Ascend Operator Debug
  short_description: Reduce and validate isolated Ascend operator failures
  default_prompt: Use $ascend-operator-debug to reduce a reproduced failure to one
    operator and compare its actual outputs against a trusted reference.
