interface:
  display_name: "NPU Fleet Monitor"
  short_description: "Locate or deploy the standalone vaws-top repository"
  default_prompt: "Use $npu-fleet-monitor to clone or locate the standalone vaws-top repository, then hand off to its project-local Agent Skill. Do not treat monitor inventory as NPU allocation authority."
