interface:
  display_name: "NPU Fleet Monitor"
  short_description: "Run the loopback-only vaws-top monitor through uvx"
  default_prompt: "Use $npu-fleet-monitor to start, inspect, restart, or stop the local vaws-top monitor via uvx, then hand off to the vaws-top CLI/MCP and its published Agent Skill. Do not treat monitor inventory as NPU allocation authority."
