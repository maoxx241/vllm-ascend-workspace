---
name: machine-management
description: Add, verify, repair, or remove a managed remote NPU host. Use for 配置服务器, 检查 ready, or container SSH repair; not code sync or serving.
---

# machine-management

Thin routing stub — the full skill definition lives at `.agents/skills/machine-management/SKILL.md`. Read that file for complete rules, decision gates, and workflow steps.

Quick entry point:

```bash
python3 .agents/skills/machine-management/scripts/machine_verify.py --machine <machine>
```
